load "std.random"
load "std.sys"
load "std.string"

clear()
if sys.os_name == "nt" do sys.system("color 02")

while true do
    lst = []
    for _ until 148 do # IDK =)))
        append(lst, random.rand_int(0, 1))
    done
    print(string.join("", lst))
done
